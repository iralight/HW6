<h1>Existing topics</h1>
<% @users.each do |user| %>
<p>
(<b><i><%=user.fname%> <%=user.lname%></i></b>)
</br>
<%= link_to("Create New Topic", new_user_topic_path(user)) %>
</br>
<%user.topics.each do |topic| %>
<%=link_to(topic.name, user_topic_path(user, topic)) %> </br>
<% end %>
</p>
<% end %>
